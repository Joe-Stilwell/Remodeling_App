# Remodeling App — Claude Instructions

Before building any widget, panel, or form, read `js/standards.js` first.
It contains the authoritative HTML patterns, CSS classes, field widths, button types, and copy-paste boilerplate for all three widget types (Form, Panel, Grid).
Never invent a class that already exists there.
